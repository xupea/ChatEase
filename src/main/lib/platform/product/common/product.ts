import { IProductConfiguration } from '../../../base/common/product';

let product: IProductConfiguration;

product = {
  version: '1000',
  data: 'now',
  applicationName: 'ChatEase',
} as unknown as IProductConfiguration;

export default product;
